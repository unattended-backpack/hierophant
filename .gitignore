hierophant.toml
contemplant.toml
.env
.act-secrets/
target/
out/
**/run-contemplant.sh

# Vendored binaries (checksums only, not the binaries themselves)
docker/moongate-server
docker/moongate-server.tar.gz
